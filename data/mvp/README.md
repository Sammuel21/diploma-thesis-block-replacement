# MVP Data

`results/logs/` contains the preserved structured output of the MVP experiment
rounds. Other datasets, caches, activations, and generated artifacts under
`data/` remain local and ignored.

The archive manifest is `docs/prototype/mvp/manifest.yml`.
